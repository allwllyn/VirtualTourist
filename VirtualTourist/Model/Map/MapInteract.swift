//
//  MapInteract.swift
//  VirtualTourist
//
//  Created by Andrew Llewellyn on 7/21/18.
//  Copyright © 2018 Andrew Llewellyn. All rights reserved.
//

import Foundation
import UIKit
import MapKit
import CoreData
import CoreLocation


class MapInteract: NSObject, MKMapViewDelegate
{
 /*   var pinArray: [MKPointAnnotation]?
    
    func dropPins(_ map: MKMapView, _ pins: [Pin])
    {
        for pin in pins
        {
            let mapPin: MKPointAnnotation?
            let coordinate = CLLocationCoordinate2D(latitude: pin.latitude, longitude: pin.longitude)
            
            mapPin?.coordinate = coordinate
            pinArray?.append(mapPin!)
        }
        map.addAnnotations(pinArray!)
    }
    
 */
    
}
